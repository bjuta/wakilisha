/**
 * @deprecated Use src/services/cultureContext instead.
 *
 * The old deterministic prose builders were removed after the Culture Context
 * Engine migration. This module is intentionally left as a tiny tombstone so
 * stale imports fail loudly during development instead of silently producing
 * public copy from the wrong layer.
 */
export {};
