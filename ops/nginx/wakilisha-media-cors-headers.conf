# Allow the production WAKILISHA application to load public Media bytes into
# an anonymous browser canvas. This snippet belongs only inside public Media
# GET/HEAD locations.
add_header Access-Control-Allow-Origin "https://wakilisha.africa" always;
