# Archived consolidated migrations

These 20 migrations consolidated granular production migrations that had already
been applied under different version numbers.

They are retained for historical review but intentionally removed from the
active top-level Supabase migration sequence.

The authoritative granular production migrations were recovered from
`supabase_migrations.schema_migrations` on 2026-07-12.
